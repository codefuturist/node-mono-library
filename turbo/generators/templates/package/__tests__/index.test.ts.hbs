import { describe, it, expect } from "vitest";
import { example } from "../src";

describe("@repo/{{name}}", () => {
  describe("example", () => {
    it("should return greeting", () => {
      const result = example();
      expect(result).toBe("Hello from @repo/{{name}}!");
    });
  });
});
