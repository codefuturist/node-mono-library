/**
 * {{description}}
 *
 * @param value - Input value
 * @returns Processed result
 *
 * @example
 * ```ts
 * import { {{name}} } from "@repo/utils/{{category}}";
 *
 * const result = {{name}}("input");
 * console.log(result);
 * ```
 */
export function {{name}}(value: string): string {
  // TODO: Implement {{name}}
  return value;
}
